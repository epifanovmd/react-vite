/**
 * Приложение задаёт голым `button`/`input` собственный кегль (`app/styles`),
 * поэтому вложенные в поле или чип элементы, чей текст должен совпадать с
 * контейнером, явно наследуют шрифт целиком.
 */
export const INHERIT_FONT_CLASS = "[font:inherit]";
